<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".KairosApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KairosApplication">
        <activity
            android:name=".ui.widget.settings.WidgetBackgroundActivity"
            android:exported="false"
            android:theme="@style/Theme.KairosApplication" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:theme="@style/Theme.KairosApplication"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <receiver
            android:name=".notification.NotificationReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.example.kairosapplication.ACTION_SHOW_REMINDER" />
                <action android:name="com.example.kairosapplication.ACTION_SHOW_REFLECTION" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".notification.TaskReminderReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.example.kairosapplication.ACTION_TASK_REMINDER" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".notification.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".widget.Widget3x1WeekInteractionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.example.kairosapplication.action.WIDGET_3X1_WEEK_PREV" />
                <action android:name="com.example.kairosapplication.action.WIDGET_3X1_WEEK_NEXT" />
                <action android:name="com.example.kairosapplication.action.WIDGET_3X1_WEEK_SELECT_DAY" />
            </intent-filter>
        </receiver>

        <service
            android:name=".widget.WidgetRemoteViewsService"
            android:exported="false"
            android:permission="android.permission.BIND_REMOTEVIEWS" />

        <service
            android:name=".widget.Widget3x3CalendarRemoteViewsService"
            android:exported="false"
            android:permission="android.permission.BIND_REMOTEVIEWS" />

        <receiver
            android:name=".widget.KairosWidgetProvider1x1"
            android:exported="true"
            android:label="@string/widget_picker_label_1x1">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info_1x1" />
        </receiver>

        <receiver
            android:name=".widget.KairosWidgetProvider3x1"
            android:exported="true"
            android:label="@string/widget_picker_label_3x1">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info_3x1" />
        </receiver>

        <receiver
            android:name=".widget.KairosWidgetProvider3x3"
            android:exported="true"
            android:label="@string/widget_picker_label_3x3">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info_3x3" />
        </receiver>

        <receiver
            android:name=".widget.WidgetAlarmReceiver"
            android:exported="false" />

        <receiver
            android:name=".widget.WidgetTaskToggleReceiver"
            android:exported="false" />
    </application>

</manifest>
